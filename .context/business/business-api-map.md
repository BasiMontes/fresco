> **Generated by**: `/business-api-map` slash command
> **Last update**: 2026-08-29
> **Last verified against**: the running codebase — `supabase/functions/**`, `app/api/**`, `supabase/migrations/**`, `lib/api/**`, `lib/stripe.ts`, `api/schemas/*.types.ts`, `.context/SRS/api-contracts.md` (no `api/openapi.json` exists — see §7; `bun run api:sync` is an interactive spec-downloader with no non-interactive mode and nothing to point it at here — see §7 item 1). `bun run api:sync` was attempted this pass and hung awaiting stdin (killed) — not applicable to this repo. Deployed Edge Functions cross-checked via Supabase MCP `list_edge_functions` (`jdqemhewjrjuopssdurn`), 2026-08-29 — this caught and corrected a stale "not deployed" claim about `delete-catalog-recipe` (see §7 item 7).
> **Sources**: `.context/business/business-data-map.md` (this pass's regeneration started against the pre-`FRESCO-301` worktree checkout, per the run's own instructions; its own `FRESCO-301`/`ADR-0015` regeneration — PR #184 — landed on `dev` mid-pass and was cross-checked directly via `git show origin/dev:...` before finalizing this document, so both files agree), `.context/business/business-feature-map.md` (2026-08-29, FRESCO-306), `.context/ADR/ADR-0001`…`ADR-0015`, `.context/PRD/user-journeys.md`, `.context/SRS/`, `package.json`
> **Scope**: full-system — running codebase. Regeneration triggered by `FRESCO-301` (daily Stripe subscription reconciliation job, `ADR-0015`, commit `45a0a95`), the only commit touching the API surface since the 2026-08-28 pass (`c4aea71`).
> **Regenerate with**: `/business-api-map` (CREATE mode if deleted, UPDATE mode otherwise — never auto-overwrites)

# Business API Map: Fresco

---

## 1. Executive Summary

An authenticated household turns a declared diet profile into a shoppable week in a handful of calls: request a menu (`generate-meal-plan`), request the matching shopping list (`generate-shopping-list`), and — across the week — tap a few `cocinada`/`descartada` toggles (`update-recipe-status`) that quietly make next week's menu better *if the household pays for Pro*. A first-time visitor does all of this with **no signup** — a real Supabase anonymous session is created transparently (`signInAnonymously()`, `ADR-0003`), and every Edge Function and RLS policy already accepts it because "authenticated" in this system has only ever meant "has a Supabase Auth JWT", never "has a password". She can later convert that guest session to a permanent account keeping the same `user_id`, or — if the email is already taken — reassign her generated data to the existing account after proving its password (`ADR-0004`).

What makes this API interesting from a business standpoint isn't its endpoint count — it's that `generate-meal-plan` **forks behavior by subscription tier without exposing that fork as a different endpoint, scope, or request field**. Free and Pro call the identical URL with the identical body; the only difference is a server-side `plan` read that decides whether three extra SQL history reads run and feed the deterministic menu-selection algorithm (`ADR-0001`, `ADR-0006`, `ADR-0008`). That branch *is* the Pro subscription's technical justification. Subscription state itself is set entirely out-of-band: Stripe calls `POST /api/stripe/webhook` (signature-authenticated, no user JWT) and that handler is the **primary** writer of `user_profiles.plan` (`ADR-0007`). Since `ADR-0015` there is a second, deliberately narrow writer: a daily `pg_cron` job hits `GET /api/cron/stripe-reconcile` to correct any drift a lost or failed webhook delivery would otherwise leave undetected — it derives state from Stripe only, never invents it, and writes zero rows when the webhook is healthy.

The backend is two surface classes, not one. Eight **Supabase Edge Functions** (Deno, separate runtime — confirmed `ACTIVE` via Supabase MCP `list_edge_functions` this pass, correcting the prior pass's "7, one undeployed" claim: `delete-catalog-recipe` has been live since 2026-08-28) own the food-safety-critical and cross-user-privileged operations; **Next.js Route Handlers** own Stripe and the RLS-scoped data export. Alongside them, several flows skip the backend entirely and call Postgres directly (RLS-scoped RPCs and table writes) for pure per-household state — favorites, the purchased-item toggle, personal recipes, deleting a week. Menu selection and shopping-list classification are deterministic in-process algorithms: **there is no external LLM call anywhere in production** (`ADR-0005`).

---

## 2. Permission & Auth Model

Fresco's authorization is **row-ownership via `auth.uid()`, not roles** — there is no `admin`/`user` column anywhere in the schema (`.context/SRS/architecture.md` §5; `domain-glossary.md` §1 "RLS"). The tiers below are the real boundaries a caller crosses in code.

| Tier | Who it applies to | How to acquire | Where enforced (code path) |
|---|---|---|---|
| **Public / unauthenticated** | Any visitor | — | `/` (landing), `/qa` (testability guide), `GET /auth/confirm` (verifies the Supabase token itself, `app/auth/confirm/route.ts`), and `POST /api/stripe/webhook` (authenticated by Stripe signature, not a session) |
| **Anonymous (guest)** | First-time visitor | `supabase.auth.signInAnonymously()` on `/onboarding` mount (`app/onboarding/page.tsx`, `ADR-0003`) | Identical to Authenticated — an anonymous JWT carries a real `auth.uid()` with `is_anonymous = true`; `requireAuthenticatedUser()` (`supabase/functions/_shared/auth.ts`) accepts it unmodified; every RLS policy is `auth.uid()`-scoped |
| **Authenticated** | Any signed-up user — Free, Pro, or Family, no distinction at this layer | Supabase Auth login/signup issues a JWT; the frontend attaches `Authorization: Bearer <jwt>` (Edge Functions) or a cookie-scoped SSR client (Route Handlers) | Every Edge Function calls `requireAuthenticatedUser()` first → `401 { "error": "No autorizado" }`. Route Handlers call `supabase.auth.getUser()` on the SSR client → `401` |
| **Owner-scoped** (sub-condition of Authenticated) | An authenticated user acting on her *own* rows | Same JWT; ownership checked by joining the target row to `auth.uid()` | `update-recipe-status` joins the slot to `meal_plans.user_id` → `403`; `generate-shopping-list` filters the plan by `user_id` → `404`; `swap_meal_plan_slots()` / `jsonb_set_comprado()` / `jsonb_add_item()` re-check `auth.uid()` inside the SECURITY DEFINER body; RLS `*_own` policies back every direct table call |
| **Admin allowlist** | The founder only | `user.id` must be in the `ADMIN_USER_ID` env allowlist (comma-separated) | `requireAdminUser()` (`supabase/functions/_shared/admin.ts`) → `403`. Gates `delete-catalog-recipe` and `/admin/recipes`. **Not** a DB role — an env-var allowlist (FRESCO-237) |
| **Service role** | Server code that must cross the row-ownership boundary | `SUPABASE_SERVICE_ROLE_KEY` (Edge Function client / `lib/supabase/service.ts`) — bypasses RLS, but **not** triggers | `reassign_guest_data()` (`EXECUTE` granted only to `service_role`); `get_push_subscriptions_without_current_plan()` (same); `auth.admin.deleteUser()` in `delete-account` / `reassign-guest-data`; the Stripe webhook's `user_profiles` writes; `delete-catalog-recipe`'s `recipes` write |
| **Service secret key** (`apikey` header) | The `pg_cron` job that invokes an Edge Function over HTTP | `SUPABASE_SECRET_KEYS.default` (`sb_secret_...`), read from Supabase Vault by the cron migration, sent on `apikey` | `requireServiceRoleCaller()` — `send-weekly-reengagement-push` is deployed `verify_jwt: false` and does its own check → `401` (`ADR-0011`, `20260823234500`) |
| **Cron secret** (`Authorization: Bearer`) | The `pg_cron` job that invokes a Next.js Route Handler over HTTP | `CRON_SECRET` — a plain shared secret (not a Supabase key), set in the Vercel Production scope and mirrored into Supabase Vault for the scheduling migration to read | `GET /api/cron/stripe-reconcile` compares `Authorization` to `` `Bearer ${process.env.CRON_SECRET}` `` → `401` on mismatch or missing Vault secret (`ADR-0015`, `20260828174500`). Distinct from the Service secret key tier above: a Route Handler target, a Bearer header (not `apikey`), and a secret unrelated to Supabase's own keys |
| **Stripe webhook signature** | Stripe's servers only | `STRIPE_WEBHOOK_SECRET_{DEV,PRE,PROD}` (per-environment, `resolveWebhookSecret()`) | `stripe.webhooks.constructEvent(rawBody, sig, secret)` in `app/api/stripe/webhook/route.ts` → `400` on failure |

**Not a tier — a business-logic branch:** Free, Pro, and Family users are all the same `Authenticated` principal. `generate-meal-plan/index.ts` reads `profile.plan` and, for `'pro'` / `'family'`, runs three extra SECURITY DEFINER history reads that feed the (same) deterministic selector; Free never calls them. No auth scope, claim, endpoint, or RLS policy distinguishes the tiers — model it as a `plan`-column branch, never a permission (`ADR-0001`; `domain-glossary.md` §4 anti-glossary).

### Token flow — the primary scheme (Supabase JWT, guest and registered)

```
  User            Supabase Auth          Frontend (Next.js)        Edge Function (Deno)
   │                    │                       │                        │
   │ 1a. signInAnonymously()  (guest, on /onboarding mount)              │
   │    OR 1b. signUp / signInWithPassword  (registered)                 │
   ├───────────────────▶│                       │                        │
   │                    │ 2. issue JWT          │                        │
   │                    │   (is_anonymous=t|f)  │                        │
   │                    ├──────────────────────▶│  (held client-side by  │
   │                    │                       │   @supabase/ssr)       │
   │ 3. action (generate menu, toggle cooked, …)│                        │
   ├────────────────────┼──────────────────────▶│ 4. Authorization:      │
   │                    │                       │    Bearer <jwt>        │
   │                    │                       ├───────────────────────▶│
   │                    │                       │                        │ 5. supabase.auth
   │                    │◀──────────────────────┼────────────────────────┤    .getUser()
   │                    │ 6. user | error       │                        │    validates
   │                    ├──────────────────────▶│                        │
   │                    │                       │                        │ 7a. valid → handler
   │                    │                       │◀───────────────────────┤ 7b. invalid → 401
   │                    │                       │   (lib/api/edge-        │    "No autorizado";
   │                    │                       │    functions.ts routes │    client redirects
   │                    │                       │    a 401 to /login?     │    to /login on 401
   │                    │                       │    session_expired=1)   │
```

### Second scheme — Stripe webhook signature (inbound, no session)

```
   Stripe ──POST /api/stripe/webhook──▶ Next.js Route Handler
                                          │ request.text()  (raw bytes — required for verification)
                                          │ stripe.webhooks.constructEvent(raw, stripe-signature, resolveWebhookSecret())
                                          │   · verified → route on event.type → service_role UPDATE user_profiles
                                          │   · unverified → 400, nothing written
                                          ▼
                                        200 { received: true }   (always, once verified — even on a downstream error)
```

### Third scheme — service secret key (inbound, `pg_cron` only)

```
   pg_cron ──net.http_post(apikey = vault secret)──▶ send-weekly-reengagement-push  (verify_jwt: false)
                                                       requireServiceRoleCaller(): apikey == SUPABASE_SECRET_KEYS.default ? proceed : 401
```

### Fourth scheme — cron secret, Bearer (inbound, `pg_cron` → Next.js only)

```
   pg_cron ──net.http_get(Authorization: Bearer <vault secret>)──▶ GET /api/cron/stripe-reconcile
                                                                     header === `Bearer ${CRON_SECRET}` ? proceed : 401
```

Same triggering actor as the third scheme (`pg_cron`), but a different target class (a Next.js Route Handler, not a Supabase Edge Function) and a different header shape (`Authorization: Bearer`, not `apikey`) — `ADR-0015` chose a Route Handler specifically so the reconciler could reuse `lib/stripe.ts`'s webhook-mapping helpers directly instead of reimplementing them in Deno.

There is no refresh-token recipe, session-cookie scheme for the API, or service-to-service OAuth exchange beyond the above. `@supabase/ssr` handles JWT refresh transparently client-side.

---

## 3. Critical Business Journeys

Six journeys, plus one sub-flow. Selected for: signup-free core value (J1), the Free/Pro fork (J2, J3), the cross-user-blast-radius write (J3), revenue (J4), and the two scheduled/inbound machine-to-machine paths (J5, J6). The food-safety guardrail is traced as a sub-flow of J2/J3 because a defect there is a health/safety incident, not a UX bug.

### Journey 1 — Guest happy path: onboard → menu → keep it

**Business purpose:** show a first-time visitor a full week of meals before asking for anything — the entire premise of Guest Mode (EPIC-FRESCO-6) and Progressive Signup (EPIC-FRESCO-7).

```
Client (/onboarding)        Supabase Auth        generate-meal-plan        Postgres
  │ 1. signInAnonymously() ─────▶│                      │                     │
  │◀── anon JWT ────────────────┤                      │                     │
  │ 2. upsertUserProfile() ─────┼──────────────────────┼────────────────────▶│ insert user_profiles (RLS own)
  │ 3. generateMealPlan(jwt) ───┼─────────────────────▶│ 4. get_filtered_    │
  │                             │                      │    recipes → 422 if │
  │                             │                      │    < 21 survive     │
  │                             │                      │ 5. selectMenu()      │
  │                             │                      │ 6. insert plan +     │
  │                             │                      │    ≤21 slots         │
  │◀── enriched menu ───────────┼──────────────────────┤                     │
  │ 7. → /menu ; guest_save_menu_banner shown after she's seen it            │
  │ 8. (later) /signup → updateUser({email}) → verifyOtp → updateUser({password})
  │      same auth.uid() preserved  →  her menu survives untouched
```

**Endpoints involved:** `POST /generate-meal-plan`; Supabase Auth API (`signInAnonymously`, `updateUser`, `verifyOtp`); direct `user_profiles` upsert. See OpenAPI substitute `.context/SRS/api-contracts.md` §1.
**Entities touched:** `auth.users`, `user_profiles`, `recipes`, `meal_plans`, `meal_plan_recipes` — `business-data-map.md` §2.
**Feature IDs:** FEAT-001 (Guest Anonymous Session), FEAT-003 (Guest→Account OTP conversion), FEAT-013 (3-step onboarding), FEAT-016 (Weekly Menu Generation), FEAT-056 (Progressive Signup Prompt) — `business-feature-map.md` §2.
**Why the steps:** step 1 must succeed before onboarding step 3 or `handleGenerate()` has no token; step 4 is FR-8.1 Layer 1 and fails closed; the conversion in step 8 keeps the same `user_id` so no data move is needed — unless the email is taken, which routes into Journey J1b.

### Journey 1b — Guest data reassignment on email conflict

**Business purpose:** a guest whose chosen email already belongs to a different account is not stuck at a dead end (`ADR-0004`, FRESCO-20).

```
/signup (email_exists branch)       reassign-guest-data Edge Fn        Postgres
  │ POST {email, password} + guest anon JWT ──▶│                          │
  │                        1. caller.is_anonymous ? else 400              │
  │                        2. fresh anon client signInWithPassword(email,pw)
  │                           → 401 if wrong (no email enumeration)        │
  │                        3. service_role reassign_guest_data(from,to) ──▶│ move meal_plans
  │                           (skip weeks the target already has)          │ + shopping_lists,
  │                        4. service_role auth.admin.deleteUser(guest.id) │ delete guest profile
  │◀── { reassigned: true } ──────────────────┤                          │
  │ 5. client signInWithPassword(email,pw) → /menu
```

**Endpoints:** `POST /reassign-guest-data`. **Entities:** `meal_plans`, `shopping_lists`, `user_profiles`, `auth.users`. **Feature ID:** FEAT-004. **Security:** the only cross-user write in the system; `reassign_guest_data()` `EXECUTE` is `service_role`-only, and the Edge Function verifies the target password before calling it.

### Journey 2 — Weekly Menu Generation (Free tier)

**Business purpose:** deliver the core promise — a full week of meals, sub-second, at zero cost, stateless by design.

```
Client            generate-meal-plan Edge Fn                     Postgres
  │ POST {semana_iso, fecha_inicio} + JWT ─▶│                        │
  │                    1. requireAuthenticatedUser() — 401           │
  │                    2. load user_profiles ──────────────────────▶│ 404 if absent
  │                    3. existing plan this week? ─────────────────▶│ 409
  │                    4. get_filtered_recipes(uid) ───────────────▶│ SQL pre-filter,
  │                       < 21 → 422                                 │ 422 (Layer 1)
  │                    5. plan='free' → recentRecipeIds = []         │  (no history read)
  │                    6. selectMenu() — deterministic, in-process   │
  │                    7. insert meal_plans + ≤21 meal_plan_recipes ▶│ compensating
  │◀── enriched menu + advertencias ────────┤                        │ delete on slot error
```

1–3. Auth, profile (`404` — onboarding must have run), no-silent-overwrite (`409`; the user resolves it via `deleteMealPlan()`, Journey J-aux).
4. **FR-8.1 Layer 1** — `get_filtered_recipes()` structurally removes allergen/diet/disliked-ingredient conflicts before selection; `422` if fewer than 21 survive. *Why:* since Gemini's removal this is the single food-safety enforcement point (see the guardrail sub-flow).
5. **The Free fork, structurally:** `plan='free'` → the three history reads are never issued; `recentRecipeIds` is `[]` (`ADR-0001`). This is the exact mechanism behind "Free never gets smarter."
6–7. Deterministic selection (`menu-selector.ts`) then persistence; a slot with no safe candidate becomes `NO_SAFE_RECIPE_SENTINEL` + an `advertencias` entry, not a hard failure.

**Endpoints:** `POST /generate-meal-plan` — `.context/SRS/api-contracts.md` §1; types in `api/schemas/meal-plan.types.ts`.
**Entities:** `user_profiles`, `recipes`, `meal_plans`, `meal_plan_recipes` — `business-data-map.md` §2, Flow 2.
**Feature IDs:** FEAT-016, FEAT-017 (quality heuristics), FEAT-019 (missing-safe-recipe warning), FEAT-020 (generate for a specific week).

### Journey 3 — Weekly Menu Generation (Pro tier) + the learning write path

**Business purpose:** the same promise plus the one mechanism Fresco charges €4.99/month for — and the write path that feeds it, which is the highest-blast-radius authorization check in the system.

```
Client        generate-meal-plan Edge Fn                        Postgres
  │ POST + JWT ──▶│  1-4. identical to Journey 2                     │
  │               │  5. plan in ('pro','family') → Promise.all([     │
  │               │       get_recent_recipe_marks(uid, 2 weeks),     │ SECURITY DEFINER,
  │               │       get_user_recipe_engagement(uid) ])  ──────▶│ auth.uid() checked
  │               │  6. selectMenu({ recentRecipeIds, userEngagement})│
  │               │     · cocinada/descartada last 2w REMOVED from    │
  │               │       candidate pool (hard filter, ADR-0005)      │
  │               │     · personal engagement nudges scoreRecipe()    │  (ADR-0008)
  │               │  7. get_user_cooked_recipe_ids(uid) →            │
  │               │     buildLearningExplanation()  (deterministic)   │
  │               │  8-10. insert; response carries explicacion_      │
  │◀── menu + explicacion_aprendizaje card ─┤     aprendizaje (Pro)   │

  ── the feeder write (update-recipe-status) ──────────────────────────
Client        update-recipe-status Edge Fn        meal_plan_recipes   recipe_learning_trigger
  │ POST {meal_plan_recipe_id, estado, rating?} + JWT ─▶│                │
  │              1. auth — 401                          │                │
  │              2. join slot → meal_plans.user_id      │                │
  │              3. slot.user_id != caller → 403  ◀── the security-critical step
  │              4. estado already terminal → 409       │                │
  │              5. UPDATE estado ─────────────────────▶│ 6. AFTER UPDATE ▶ veces_cocinada++
  │◀── { ok: true, estado } ───────────────────────────┤   (global aggregate on recipes)
```

**Why step 3 of the feeder matters:** without the ownership join, any authenticated user could mutate any slot and pollute the **global** aggregate (`recipes.veces_cocinada` etc.) that every Pro user's next generation reads — a cross-user, product-wide blast radius, not a single-account one. The terminal-state `409` (step 4) prevents double-counting the same signal.
**Why the Pro reads are three separate functions:** `get_recent_recipe_marks` (2-week window, exclusion), `get_user_recipe_engagement` (all-time counts, scoring nudge), `get_user_cooked_recipe_ids` (all-time set, "destacadas" for the explanation) — different windows and semantics, deliberately not one parameterized function (`ADR-0006`, `ADR-0008`).

**Endpoints:** `POST /generate-meal-plan`, `POST /update-recipe-status` — `.context/SRS/api-contracts.md` §1, §4.
**Entities:** as Journey 2, plus `meal_plan_recipes` (write) and `recipes` (trigger-driven aggregate) — `business-data-map.md` §4 (state machine), §5 (triggers).
**Feature IDs:** FEAT-018 (Pro 2-week no-repeat), FEAT-025 (mark cooked/discarded), FEAT-029 (aggregate learning stats), FEAT-030 (Pro history-informed generation), FEAT-031 (learning explanation card), FEAT-032 (free-tier learning notice).

### Journey 4 — Pro subscription lifecycle (Stripe Checkout + webhook)

**Business purpose:** self-serve upgrade to Pro — the epic (FRESCO-227) that reversed the MVP-scope deferral of self-serve payment (`ADR-0007`). Revenue-critical.

```
/profile CTA ──▶ POST /api/stripe/checkout        Stripe            POST /api/stripe/webhook
  │ (SSR session — 401 if none)  │                   │                        │
  │  stripe.checkout.sessions.create({               │                        │
  │    mode:'subscription', price=STRIPE_PRICE_ID_PRO_MONTH,                   │
  │    subscription_data:{trial_period_days:7},                               │
  │    payment_method_collection:'if_required',                              │
  │    client_reference_id = auth.uid() })  ────────▶│ hosted page            │
  │◀── { url } ; client redirects ──────────────────┤                        │
  │                                                  │ checkout.session.      │
  │                                                  │ completed ────────────▶│ verify price ==
  │                                                  │ customer.subscription. │ Pro (else refuse)
  │                                                  │ updated / deleted ────▶│ service_role
  │  return page re-reads user_profiles.plan          │                        │ UPDATE user_profiles
  │  (never writes)                                   │                        │ (plan, stripe ids,
  │                                                  │                        │  plan_expires_at,
  │                                                  │                        │  payment_failed_at)
  ── manage/cancel ──▶ POST /api/stripe/portal (plan must be 'pro' + stripe_customer_id → else 404)

  ── reconciliation sub-flow (ADR-0015, FRESCO-301) — drift correction, not a normal-path step ──
  pg_cron (04:17 UTC daily) ──net.http_get(Authorization: Bearer <vault secret>)──▶ GET /api/cron/stripe-reconcile
    1. Bearer != CRON_SECRET → 401 (job no-ops on schedule, self-heals once the secret is set)
    2. for each user_profiles row with a stripe_subscription_id:
         stripe.subscriptions.retrieve(id) → resolveReconciledState()  (same mapper the webhook uses)
         missing subscription (deleted in Stripe, webhook never told us) → treat as a downgrade
    3. diff vs current row; write ONLY the columns that differ, via service_role
         · never re-grants 'pro' for a non-Pro price (mirrors the webhook's price-id guard)
         · never rewrites stripe_customer_id / stripe_subscription_id
         · downgrade path leaves plan_expires_at untouched — byte-for-byte the webhook's own write
    4. { checked, reconciled, drifted[] }  — 0 reconciled in steady state (healthy webhooks)
```

**Why webhook-only, mostly:** two *routine* writers (client + webhook) would race a failed/reversed payment into a stuck `plan:'pro'`. The `prevent_client_subscription_writes()` DB trigger enforces single-writer even against a compromised client (`ADR-0007`, `20260818190000`). `ADR-0015` (2026-08-28, FRESCO-301) deliberately adds a *second* writer anyway — but one constrained to only ever correct drift *toward* what Stripe already says, never to originate state: the gap it closes is a webhook delivery that Stripe's own ~3-day retry window still lost, which before this pass left `user_profiles` silently and permanently wrong (this map's own prior-pass Discovery Gap — see §7). Full state machine: `business-data-map.md` §4.
**Endpoints:** `POST /api/stripe/checkout`, `POST /api/stripe/portal`, `POST /api/stripe/webhook`, `GET /api/cron/stripe-reconcile`. No OpenAPI — see `app/api/stripe/*/route.ts`, `app/api/cron/stripe-reconcile/route.ts`.
**Entities:** `user_profiles` (subscription columns) — `business-data-map.md` §2. **Feature IDs:** FEAT-059–063 (`business-feature-map.md` §2, Subscription & Billing domain — FEAT-063 is the reconciliation job).

### Journey 5 — Weekly re-engagement web push (scheduled, machine-to-machine)

**Business purpose:** nudge the "planificar cada domingo" habit for users who haven't planned this week (FRESCO-241).

```
pg_cron (Sun 18:00 UTC) ──net.http_post(apikey = vault secret)──▶ send-weekly-reengagement-push  (verify_jwt:false)
  1. requireServiceRoleCaller() — apikey == SUPABASE_SECRET_KEYS.default ? else 401
  2. VAPID setup (VAPID_* Edge Function secrets) — else 500
  3. get_push_subscriptions_without_current_plan(iso_week)  (service_role-only RPC)
  4. per row: webpush.sendNotification(...)  ──▶ browser push service (FCM/Mozilla/…)
       404/410 → DELETE push_subscriptions row ; other error → log + skip
  5. { semana_iso, users_targeted, notifications_sent, stale_subscriptions_removed }
```

Opt-in is a per-device toggle on `/profile` that inserts a `push_subscriptions` row directly (RLS `own`, `lib/api/push-subscriptions.ts`).
**Endpoints:** `send-weekly-reengagement-push` (no browser caller); direct `push_subscriptions` insert/delete. **Entities:** `push_subscriptions`, `meal_plans`. **Feature ID:** FEAT-067. **ADRs:** `ADR-0011` (scheduler), `ADR-0012` (`web-push`).

### Journey 6 — Shopping list for the week

**Business purpose:** turn the menu into a shoppable, aisle-ordered, costed list.

```
Client ──▶ POST /generate-shopping-list {meal_plan_id} + JWT
  1. auth — 401
  2. plan.user_id == caller — 404
  3. list already exists — 409
  4. load slots' recipes → consolidateIngredientes() (deterministic) → 422 if empty
  5. classifyShoppingList() (static aisle map + per-unit price) → coste_estimado_min/max
  6. insert shopping_lists.items (jsonb)
  ── later ──▶ jsonb_set_comprado() RPC (toggle) / jsonb_add_item() RPC (add a suggestion) — direct, RLS-scoped
  ── suggestions ──▶ POST /get-shopping-list-suggestions {shopping_list_id} — favorites-derived, real data only
```

**Endpoints:** `POST /generate-shopping-list`, `POST /get-shopping-list-suggestions`; RPCs `jsonb_set_comprado`, `jsonb_add_item`. — `.context/SRS/api-contracts.md` §2–§3; `api/schemas/shopping-list.types.ts`.
**Entities:** `meal_plans`, `meal_plan_recipes`, `recipes`, `shopping_lists`, `favorites`. **Feature IDs:** FEAT-026–028, FEAT-194-carousel.

### Sub-flow (of J2/J3) — Food-safety guardrail

**Business purpose:** a declared allergen or hated ingredient must never reach a plate via a generated menu — a failure here is a health/safety incident.

```
generate-meal-plan                              Postgres
  get_filtered_recipes(user_id) ──────────────▶ structural SQL exclusion:
    not (recipes.alergenos ?| profile.alergenos)
    AND (dieta_* flags satisfied)
    AND not (recipes.ingredientes_principales ?| profile.ingredientes_odiados)
  IF < 21 survive → 422 (hard stop — no degraded menu is ever generated)
  IF a single slot's scored pool is empty → NO_SAFE_RECIPE_SENTINEL
    + a templated advertencias entry → surfaced as a prominent AlertBanner (FR-8.2)
```

**Now single-layer.** The prior design had a second, model-prompt layer ("REGLAS ABSOLUTAS"); it went away with Gemini (`ADR-0005`; `business-feature-map.md` FEAT-058 flags this explicitly). `get_filtered_recipes()` is the sole enforcement point, and it fails closed. The founder-run manual food-safety checklist (FR-8.3) is an additional, out-of-band backstop, not part of the API.
**Code:** `supabase/migrations/20260725120100_create_fresco_core_tables.sql` (`get_filtered_recipes`), `20260726000000_fix_get_filtered_recipes_keto_halal.sql`, `20260804010000_add_recipe_id_param_to_get_filtered_recipes.sql`.
**Feature IDs:** FEAT-058 (allergen), FEAT-059 (hated-ingredient).

---

## 4. Architecture Behind the API

```
   Client (Next.js 16 App Router · React 19 · Vercel)
     · RSC pages read Postgres via the cookie-scoped SSR client (lib/supabase/server.ts)
     · Client components call Edge Functions (lib/api/edge-functions.ts) and RPCs (lib/api/*.ts)
          │                                    │                              │
          │ Authorization: Bearer <jwt>        │ SSR cookie session           │ supabase.rpc / .from()
          ▼                                    ▼                              ▼
   ┌─────────────────────────────┐   ┌──────────────────────────┐   ┌────────────────────────────┐
   │ Supabase Edge Functions      │   │ Next.js Route Handlers    │   │ Direct-to-Postgres          │
   │ (Deno, deployed separately)   │   │ (Vercel, same deploy as   │   │ (RLS-scoped, no backend)     │
   │ verify_jwt: true  ×7:         │   │  the frontend)            │   │ RPCs: get_filtered_recipes,  │
   │  generate-meal-plan           │   │  POST /api/stripe/checkout │   │  swap_meal_plan_slots,       │
   │  generate-shopping-list       │   │  POST /api/stripe/portal   │   │  jsonb_set_comprado,         │
   │  update-recipe-status         │   │  POST /api/stripe/webhook  │   │  jsonb_add_item              │
   │  reassign-guest-data          │   │  GET  /api/profile/export  │   │ Tables: favorites,           │
   │  delete-account               │   │  GET  /api/cron/           │   │  recetas_propias,            │
   │  get-shopping-list-suggestions │   │    stripe-reconcile        │   │  user_profiles (non-sub),    │
   │  delete-catalog-recipe        │   │  GET  /auth/confirm        │   │  meal_plans DELETE,          │
   │   (admin-gated)               │   │  (Supabase Auth API for    │   │  push_subscriptions          │
   │ verify_jwt: false ×1:         │   │   signup/login/OTP/reset)  │   │                             │
   │  send-weekly-reengagement-push │   │                           │   │                             │
   └──────────────┬───────────────┘   └───────────┬──────────────┘   └──────────────┬─────────────┘
                  │                                │                                 │
                  ▼                                ▼                                 ▼
   ┌───────────────────────────────────────────────────────────────────────────────────────────┐
   │ Supabase Postgres — ONE project (jdqemhewjrjuopssdurn), shared by local / staging / prod   │
   │  · 9 tables (recipes 1000 rows; user_profiles; meal_plans; meal_plan_recipes; shopping_    │
   │    lists; recetas_propias; favorites; push_subscriptions; auth.users)                       │
   │  · RLS on every user table (auth.uid()); SECURITY DEFINER functions for cross-cutting logic │
   │  · recipe_learning_trigger, prevent_client_subscription_writes, handle_updated_at ×6        │
   │  · pg_cron + pg_net (4 scheduled jobs, incl. 1 external-URL target — ADR-0015) · supabase_vault │
   │    (cron secrets) · pg_trgm (recipe search)                                                  │
   └───────────────────────────────────────────────────────────────────────────────────────────┘
                  │ outbound
                  ▼
   Stripe API · PostHog EU · Sentry · browser Web Push services      (NO external LLM — ADR-0005)
```

| Component | Role | Persistence / Integrations touched | Why it matters for dev |
|---|---|---|---|
| Client (Next.js / Vercel) | RSC pages do RLS-scoped reads directly; client components call Edge Functions + RPCs; holds the JWT via `@supabase/ssr` | Postgres (RLS reads), Edge Functions, Route Handlers | Frontend and Edge Functions are **separate deploys** — an Edge Function fix doesn't need a frontend redeploy. A `401` from any Edge Function is caught once in `lib/api/edge-functions.ts` and redirects to `/login?session_expired=1` |
| Supabase Edge Functions (Deno) | Auth, ownership checks, orchestration, deterministic selection/consolidation, persistence, and every cross-user-privileged operation | Postgres (read/write via request-scoped or service-role client), `web-push` (one function) | **This is where a new food-safety-critical or cross-user endpoint belongs.** Unit-testing one means mocking a Supabase client — there is no external HTTP to mock any more (Gemini is gone). `_shared/` holds `auth.ts`, `admin.ts`, `cors.ts`, `http.ts`, `service-role-client.ts` |
| Next.js Route Handlers | Stripe session creation + webhook + reconciliation cron, RLS-scoped data export, auth-email confirmation | Stripe SDK (`lib/stripe.ts`), Postgres (SSR client for reads, `lib/supabase/service.ts` for the webhook's and reconciler's writes) | New payment/billing logic and anything needing Node APIs or the Stripe SDK goes here, not an Edge Function — `GET /api/cron/stripe-reconcile` (`ADR-0015`) reuses the webhook's own `lib/stripe.ts` mappers for exactly this reason. The webhook and the reconciler are the only places `lib/supabase/service.ts` (RLS-bypassing) may be imported from a Route Handler |
| Direct-to-Postgres (RPC + table) | Pure per-household state with no cross-cutting rule: favorites, purchased-item toggle, personal recipes, delete-a-week, push opt-in, aviso dismissal | Postgres only, RLS `*_own` policies / SECURITY DEFINER ownership re-checks | **Do not** route these through an Edge Function — there's no orchestration to own. New RPCs must follow `20260801010000_harden_security_definer_functions.sql`: explicit `revoke ... from public` then `grant` to the intended role, and an `auth.uid()` check in the body |
| Postgres (1 project) | System of record + scheduler + secret store | `pg_cron`, `pg_net`, `supabase_vault`, `pg_trgm`; `recipe_learning_trigger` owns the learning aggregate; `prevent_client_subscription_writes` owns the ADR-0007 invariant | Business rules that must hold regardless of caller live in triggers/DEFINER functions, not app code. All three environments hit the **same** DB — a staging test writes production rows |

There is no API gateway, load balancer config, or service mesh. Deploys are independent Supabase artifacts (`supabase functions deploy`) + one Vercel deploy — consistent with a solo-founder operating scale.

---

## 5. External Integrations

| Service | Trigger | Direction | Failure mode (user-visible) | Journeys affected |
|---|---|---|---|---|
| **Stripe** | `POST /api/stripe/checkout`, `POST /api/stripe/portal` | Outbound sync | Session creation 5xx → `500` "No se pudo iniciar el pago." / "No se pudo abrir la gestión…"; no state change | J4 |
| **Stripe** | `POST /api/stripe/webhook` (inbound, per-env signature) | Inbound async | Bad signature → `400`, nothing written. Missed delivery → `user_profiles` drifts from Stripe until the next daily reconciliation run corrects it (`ADR-0015` — was previously undetectable, see §7). Post-verification downstream error → logged, still `200`; recovery = manual dashboard replay | J4; J2/J3 (the `plan` read) |
| **Stripe** | `pg_cron` daily 04:17 UTC → `GET /api/cron/stripe-reconcile` (`ADR-0015`) | Outbound sync (cron-triggered) | Wrong/missing `CRON_SECRET` → `401`, job no-ops, self-heals the next run once fixed. One subscription's Stripe API error → logged + skipped, batch continues. Steady state (healthy webhooks) writes 0 rows | J4 |
| **PostHog** (EU Cloud) | `posthog-js` client provider; `posthog-node` from API routes + the Stripe webhook | Outbound async | **Silent** — every `capture`/`identify` is a no-op on failure or missing key; never surfaces to the user | none functionally (analytics) |
| **Sentry** | `instrumentation.ts` `onRequestError` + client/server/edge configs | Outbound async | **Silent** — degraded observability only; if `SENTRY_AUTH_TOKEN` is unset, prod stack traces stay minified | none functionally |
| **Browser Web Push services** (FCM / Mozilla / Apple …) | `send-weekly-reengagement-push`, per `push_subscriptions` row | Outbound sync (in the job) | `404`/`410` → that subscription's row is deleted (endpoint dead); transient 5xx/timeout → logged + skipped, row kept; one bad send never aborts the batch. User just doesn't get that week's reminder | J5 |
| **Supabase Auth** (incl. Anonymous Sign-In) | Every auth page; `signInAnonymously()` on `/onboarding` | Outbound sync | Anonymous sign-in rate-limited (default 30/h) → inline error, no silent hang. OTP/recovery email hits free-tier send limits + rejects non-standard domains → the guest-upgrade friction named in `ADR-0003` | J1, J1b, all authenticated journeys |

`GEMINI_API_KEY` and any external LLM endpoint are **not** part of the API boundary — removed 2026-08-01 (`ADR-0005`). `feature-map` §6's "Google Gemini — Removed" row is current; §5's older "Gemini is the only third-party service" framing is not.

---

## 6. Cross-References

- **Data-map entities this API exposes:** `recipes`, `user_profiles`, `meal_plans`, `meal_plan_recipes`, `shopping_lists`, `favorites`, `recetas_propias`, `push_subscriptions`, `auth.users` — all defined in `.context/business/business-data-map.md` §2 (Entity Map), with the `meal_plan_recipes.estado` and `user_profiles` subscription state machines in §4 and the trigger/cron inventory in §5.
- **Feature-map features this API backs:** every FEAT-ID cited above resolves in `.context/business/business-feature-map.md` §2; that document's §3 (CRUD matrix) and §4 (endpoint inventory) are the authoritative per-entity CRUD and literal endpoint table — not duplicated here.
- **API contract source (OpenAPI substitute):** no `api/openapi.json` exists. The contract is `api/schemas/*.types.ts` (`meal-plan`, `shopping-list`, `recipe`, `user-profile`, `api-contracts`), regenerated by `bun run api:sync` (`scripts/sync-openapi.ts`), plus the prose contract in `.context/SRS/api-contracts.md`. Re-point this section to `api/openapi.json` if one is ever produced.
- **Downstream consumers:** `/master-implementation-plan` sequences API-backed work using these journey/feature cross-references; `/sprint-development` reads the relevant journey before scoping any story that touches `generate-meal-plan`, `update-recipe-status`, `generate-shopping-list`, or the Stripe handlers.

---

## 7. Discovery Gaps

> MANDATORY — anything not verified end-to-end.

1. **No OpenAPI spec.** Every "endpoint" reference above points at `.context/SRS/api-contracts.md` or `api/schemas/*.types.ts`, not a schema registry. `bun run api:sync` is an interactive spec-downloader (URL / GitHub / local file) with no non-interactive mode and no spec source configured for this repo — it hung awaiting stdin when attempted this pass and was killed, not run to completion. It does not maintain these types from a spec; they're hand-authored. Re-run this map if a real spec appears.
2. **`generate-shopping-list` has no rate limiting yet.** `generate-meal-plan` got its 5 req/user/hour limit deployed 2026-08-28 (`FRESCO-243` / `ADR-0010` — shared `rate_limits` table + atomic `check_and_increment_rate_limit` RPC, `cleanup-expired-rate-limits` `pg_cron`, 4 test-account UIDs short-circuited). `generate-shopping-list` is the obvious next adopter of the same RPC (SRS `NFR-SEC-6`).
3. ~~Subscription state has no reconciliation job.~~ **RESOLVED 2026-08-28** — `FRESCO-301` / `ADR-0015` shipped the daily `pg_cron` → `GET /api/cron/stripe-reconcile` job (commit `45a0a95`). See Journey 4's reconciliation sub-flow and §5. Remaining open item from that ship is #4 below.
4. **`CRON_SECRET` presence not independently verified this pass.** The reconciliation route (`ADR-0015`) returns `500` if `CRON_SECRET` is unset, and `401` if the Vercel value and the Supabase Vault secret (`stripe_reconcile_cron_secret`) disagree — same class of silent-401 risk as item #8 below for the push job. `business-feature-map.md` FEAT-063 flags the same gap. **Ask**: confirm both are set and in sync in the Vercel Production scope + Vault.
5. **One Supabase project for all three environments.** `local`, `staging` (`fresco-pre.vercel.app`), and `production` (`fresco-pro.vercel.app`) all use ref `jdqemhewjrjuopssdurn` (`.agents/project.yaml`, deliberate). Any staging API call mutates production data. The live DB's 23 profiles / 43 guest sessions are a test/production mix. The reconciliation cron (`ADR-0015`) makes this sharper: it targets `fresco-pro.vercel.app` specifically because it's the one deploy guaranteed live — the first `pg_net` call in the system to leave Supabase's internal network for an external app URL.
6. **No single environment resolver.** `lib/urls.ts` was deleted (dead code). `lib/stripe.ts` `resolveWebhookSecret()` resolves the Stripe secret per `VERCEL_ENV` + branch; `_shared/cors.ts` carries its own hardcoded origin allowlist; `.agents/project.yaml` `environments:` has `local`/`staging`/`production` but no `dev`. Each concern resolves its own environment — worth consolidating.
7. **`delete-catalog-recipe` deployed 2026-08-28** with `ADMIN_USER_ID` → the new admin account (`hola.frescoapp+admin@gmail.com`) — confirmed `ACTIVE` (version 3) via Supabase MCP `list_edge_functions` this pass, correcting the architecture diagram in the 2026-08-28 version of this map, which still listed it as "in repo, NOT deployed." Full `/admin/recipes` delete flow still wants an in-app test with a real admin session.
8. **`send-weekly-reengagement-push` auth depends on a manually-created Vault secret** (`edge_function_secret_key`, `sb_secret_...`). `cron.job` shows the schedule + `apikey` header wired; the secret's actual presence in `vault.decrypted_secrets` was not verifiable this pass. If absent, the job runs weekly and every send silently 401s.
9. **`sustituida` estado has no live API caller found.** `update-recipe-status` accepts `estado='sustituida'` + `nueva_recipe_id`, but the calendar uses `swap_meal_plan_slots` for reordering and no component issuing the `sustituida` call was located. Possibly dead or planned.
10. **`update-recipe-status` is a `POST` acting as a semantic `PATCH`** (per `api-contracts.md` §4 and `business-feature-map.md` §4). Documented, but worth noting for anyone generating a client from method semantics.
11. **`/auth/confirm` open-redirect guard** rejects protocol-relative `next` paths (`//evil.com`) and only accepts in-app relative paths (`app/auth/confirm/route.ts`) — verified in code, no separate test evidence reviewed.
12. **0 Pro/Family users in the live DB** — the entire Pro API surface (the fork in J3, the Billing Portal in J4, `get_user_recipe_engagement`, the payment-failed path, and now the reconciliation job's `pro` path) is unexercised against real subscription data.
13. **Journey cap:** 6 journeys + 1 sub-flow, under the 7 default. Fresco's real surface is 8 Edge Functions + 6 Route Handlers + a handful of RPCs; a further journey would just recombine calls already traced. Guest reassignment (J1b) and the guardrail are documented as sub-flows rather than padding the count; the reconciliation job is folded into Journey 4 for the same reason, and the admin-only `delete-catalog-recipe` (Admin allowlist tier, §2) isn't a business-critical journey on its own.
14. **PRD/SRS drift**: several FRs still describe Gemini as present and `FR-8.1` as two-layer. Not corrected here — a `/project-foundation` concern. `.context/SRS/non-functional-requirements.md` §7 (Observability) is current (added FRESCO-284).
15. **`business-data-map.md` regeneration landed mid-pass — cross-checked, not a gap.** A sibling regeneration of `business-data-map.md` was running concurrently in a separate worktree when this pass started (this pass's worktree checkout of `dev` still showed the pre-`FRESCO-301` version); it merged as PR #184 before this document was finalized. Its Flow 7b and this map's Journey 4 reconciliation sub-flow independently arrived at the same mechanism, confirming both readings; its Supabase MCP-confirmed Edge Function count (8, `delete-catalog-recipe` `ACTIVE`) was pulled forward into this document (item 7 above). No open discrepancy remains between the two documents as of this merge.

---

<!-- human-notes: anything written below this comment is preserved across regenerations -->
